# Get_Next_Line
*This project has been created as part of the 42 curriculum by vde-alme.*  **Get Next Line** — A function that reads a line from a file descriptor. Developed in C using static variables, it works with any BUFFER_SIZE and supports both files and stdin. Bonus: multiple fds with a single static variable.
